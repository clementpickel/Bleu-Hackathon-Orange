# LLM Provider Configuration
# The system uses TWO different LLM configurations for optimal performance:

# ========================================
# 1. PDF EXTRACTION (Structured Data)
# ========================================
# Used by: /process endpoint for extracting structured data from PDFs
# Best models for PDF extraction - need excellent JSON output:
#   - OpenAI: gpt-4o (RECOMMENDED - excellent structured JSON, reliable)
#   - Gemini: gemini-1.5-pro (large 2M context, native JSON mode)
#   - Groq: llama-3.3-70b-versatile (very fast, decent quality, cost-effective)
#   - Grok: grok-beta (experimental, not recommended for production)

LLM_PROVIDER=openai
OPENAI_MODEL=gpt-4o

# ========================================
# 2. REASONING + FUNCTION CALLING (Upgrade Analysis)
# ========================================
# Used by: /analyze-upgrade-with-pdfs for complex reasoning and PDF function calling
# Best models for reasoning + function calling - need complex logic + tool use:
#   - OpenAI: gpt-4o (RECOMMENDED - best reasoning + function calling, most reliable)
#   - Groq: llama-3.3-70b-versatile (FAST alternative - good reasoning + function calling)
#   - OpenAI: o1-preview (best reasoning BUT no function calling - not usable here)
#   - OpenAI: o1-mini (good reasoning BUT no function calling - not usable here)
#
# Note: Analysis REQUIRES function calling to read PDFs, so o1 models cannot be used

ANALYSIS_LLM_PROVIDER=groq
ANALYSIS_LLM_MODEL=llama-3.3-70b-versatile

# ========================================
# Provider API Keys
# ========================================

# OpenAI Configuration
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4o

# Grok (xAI) Configuration
GROK_API_KEY=your_grok_api_key_here
GROK_MODEL=grok-beta

# Gemini (Google) Configuration
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-1.5-pro

# Groq Configuration (Fast Inference)
GROQ_API_KEY=your_groq_api_key_here
GROQ_MODEL=llama-3.3-70b-versatile

# Database
DATABASE_URL=postgresql+psycopg://postgres:postgres@db:5432/hackathon
