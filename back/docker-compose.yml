version: '3.8'

services:
  api:
    build: .
    container_name: bleu-hackathon-api
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app/app
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
